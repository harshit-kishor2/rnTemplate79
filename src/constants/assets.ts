import _ANIMATIONS_CONST from '@assets/animations';
import _IMAGES_CONST from '@assets/images';
import {_SVG_ICONS} from '@assets/svgs';

const ASSETS = {
  ANIMATIONS: _ANIMATIONS_CONST,
  IMAGES: _IMAGES_CONST,
  SVGS: _SVG_ICONS,
};

export default ASSETS;
